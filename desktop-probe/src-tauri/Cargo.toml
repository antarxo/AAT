[package]
name = "aat-desktop-probe"
version = "0.1.0"
description = "Diagnostic-only desktop wrapper for AAT"
authors = ["antarxo"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "desktop_factory_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
