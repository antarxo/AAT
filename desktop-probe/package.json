{
  "name": "aat-desktop-probe",
  "private": true,
  "version": "0.1.0",
  "description": "Diagnostic-only Tauri desktop wrapper for the AAT educational application.",
  "type": "module",
  "scripts": {
    "icons": "tauri icon app-icon.svg",
    "prepare": "node scripts/prepare-app.mjs",
    "check": "node scripts/check-app.mjs",
    "dev": "npm run icons && npm run prepare && tauri dev",
    "build": "npm run icons && npm run prepare && npm run check && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "uplot": "1.6.30"
  }
}
